# Recorder Garden runbook

The user authorised a large local build, requested no checkpoint archives, and deferred repository work until the completed local result. One substantial step therefore owns the complete green product; splitting it would force a publication boundary before the local result exists.

```design-lock
schema | protasis-design-evidence/v1
sha256 | 3e14cf0b87cbb370bf77ed67824c405dd66512a660dfcd2711050a69dc9b5c93
candidate | agent-proposed-schema-pages
```

## Step 1: Build the twelve-lesson recorder garden and agent proposal pipeline

**Goal.** Replace the generated placeholder with a complete static GitHub Pages-ready recorder garden containing twelve schema-bound lessons, browser-local microphone scoring, exact Baroque fingerings, a root idea inbox, provider-neutral agent proposal tooling, checked pull-request and deployment workflows, operating documentation, and the committed study and runbook.
**Entry.** The receipted run branch `fiat/local-baroque-recorder-curriculum-with-microphon` at generated base `502ed4f2fe222781d328197c6eb490ce15684851`, with no product edits, no pushed commit, an empty public `origin`, and no resolved Campbell upstream.
**Exit.** The root route demonstrates lesson one microphone scoring, a B-A-G copy lesson, one short tune, the full Baroque fingering explorer, and the adult panel; fixture mode demonstrates a pending idea becoming one schema-valid lesson proposal without a repository write; the production artefact uses relative assets; all declared checks pass under `npm run verify:local`.
**Files.** `index.html`, `package.json`, `package-lock.json`, `vite.config.ts`, `tsconfig.json`, `LESSON_IDEAS.md`, `schema/lesson.schema.json`, `content/lessons/*.json`, `src/**`, `public/**`, `scripts/**`, `tests/**`, `.github/workflows/agent-lessons.yml`, `.github/workflows/pages.yml`, `docs/decisions/*.md`, `docs/research/study.md`, `docs/research/runbook.md`, `README.md`, and `LICENSE` are created or changed; obsolete Vinext, Cloudflare, placeholder route, and unused generated dependencies are removed.
**Tests.** Add pure Node tests for the schema validator, closed catalogue, idea parser, provider request and response boundaries, changed-path allowlist, exact natural-octave fingerings, pitch detection over synthesized C5-to-C6 waves, silence and noise rejection, note classification, sequence progress, static-content restrictions, and workflow shape. The Elenchus runner is `node scripts/emit-node-test-report.mjs {report}`; its report format is `node-test-json-v1`; its report file is `.elenchus/node-test.json`. Browser microphone hardware remains a manual demo boundary and is never claimed by fixtures.
**Disciplines.** phylax: microphone frames, model responses, provider URLs, credentials, workflow tokens, generated paths, and rendered lesson strings cross trust boundaries. ephoros: microphone state, agent job results, path checks, pull-request URLs, build conclusions, and deployed URLs must remain visible without product analytics. metron: none, no runtime performance improvement claim; build sizes are descriptive only. elenchus: pitch, schema, provider, and workflow failures need reproduction and regression fixtures. hypomnema: microphone scoring, the twelve-lesson schema, agent authority, local operation, and later publication are expensive to reverse and need ADRs plus operator instructions.
