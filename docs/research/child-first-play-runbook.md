# Child-first recorder play runbook

This run follows the four modules selected in the study. Each step leaves the complete tree green and gives the next step one accepted interface to build on. The child copy boundary comes first; the viewport stage and play cards may not create text outside it. The final step demonstrates every child state and refuses integration while the public Pages entry still points at repository source.

```design-lock
schema | protasis-design-evidence/v1
sha256 | 14dccd4d03d38fd0f847b74d9e5b0cb92120d6d230d26d7a65a9b9cb880ae38c
candidate | one-screen-play-loop
```

## Step 1: Separate child and grown-up copy

**Goal.** Establish separate render roles, a closed child lexicon and an exhaustive child-copy manifest before changing the lesson interaction.
**Entry.** The receipted run branch `fiat/child-first-age-five-recorder-play` at `3c215d3aa17bfe31ea6c766b38493b170c5e5ae1`, where Node 22.19.0 `npm run verify:local` passes 67 of 67 tests and production JavaScript is 232827 bytes.
**Exit.** `npm run verify:local && node scripts/check-child-copy.mjs --candidate one-screen-play-loop --criterion rendered-child-copy-approved --report .hexaemeron/reports/conformance/one-screen-play-loop--rendered-child-copy-approved.json` exits zero; every visible or accessible child string from every declared state is present in the deterministic manifest and admitted by the reviewed lexicon; grown-up prose is conditionally unmounted from child mode; the existing lesson, progress, microphone and runtime boundaries remain green; and the accepted study, this runbook and ADR-005 are shipped in their declared documentation homes.
**Files.** Create or change `src/App.tsx`, `src/components/GrownUpSetup.tsx`, `src/components/ChildStage.tsx`, `src/lib/child-copy.ts`, `scripts/check-child-copy.mjs`, `tests/child-copy.test.mjs`, `README.md`, `docs/verification.md`, `docs/decisions/ADR-005-use-a-child-first-one-screen-play-loop.md`, `docs/research/README.md`, `docs/research/child-first-play-study.md` and `docs/research/child-first-play-runbook.md`; do not change dependencies, lesson data, schema, generated catalogue, workflow authority or persisted progress shape.
**Tests.** Add exhaustive lexical admission, punctuation, dynamic note-label, accessible-name and opposite-role rejection cases in `tests/child-copy.test.mjs`; extend static and built boundary checks so raw grown-up copy cannot enter the child bundle path; keep all existing tests green. The Elenchus runner is `node scripts/emit-node-test-report.mjs {report}`; its report format is `node-test-json-v1`; its report file is `.elenchus/node-test.json`.
**Disciplines.** phylax: the role boundary protects the child accessibility tree and refuses lesson or generated-copy bypass. ephoros: the checker emits bounded manifest counts, rejected tokens, role result and commit in CI without product telemetry. metron: this step keeps the same 300000-byte production JavaScript ceiling and makes no speed claim. elenchus: every audit fix needs a parent-red and fixed-green report from the declared runner. hypomnema: ADR-005 records the expensive-to-reverse split, closed lexicon and rejected long-page and arcade options.

## Step 2: Fit one child task to one screen

**Goal.** Replace the scrolling child document with one large, calm task card and one-action help or stop at every supported viewport.
**Entry.** The accepted Step 1 head, after its complete Exit command passes and the copy-role interface and ADR-005 are present.
**Exit.** `npm run verify:local && node scripts/check-child-layout.mjs --candidate one-screen-play-loop --criterion small-phone-no-scroll --report .hexaemeron/reports/conformance/one-screen-play-loop--small-phone-no-scroll.json` exits zero after replaying every child state at 320 by 568, 391 by 844 and 768 by 1024 CSS pixels; each state has no document or horizontal overflow, no child instruction or action text below 20 CSS pixels, no child action below 64 by 64 CSS pixels and a reachable one-action help or stop; grown-up mode may scroll and remains usable at the same widths.
**Files.** Change `src/components/ChildStage.tsx`, `src/styles.css` and `src/App.tsx`; create `scripts/check-child-layout.mjs` and `tests/child-layout-contract.test.mjs`; update `docs/verification.md` and the family setup section of `README.md`; preserve the Step 1 lexicon and role interface.
**Tests.** Add the closed child-state geometry matrix, dynamic viewport, safe-area, horizontal orientation, enlarged-text, reduced-motion, focus and clipped-stop cases; the layout checker must launch the built app in a bounded headless browser and retain exact viewport measurements in its named report. The Elenchus runner is `node scripts/emit-node-test-report.mjs {report}`; its report format is `node-test-json-v1`; its report file is `.elenchus/node-test.json`.
**Disciplines.** phylax: child state and viewport inputs are closed enumerations and reports retain no child action data. ephoros: the report names state, viewport, scroll dimensions, smallest text, smallest target and exit-action count. metron: compare the same three viewport geometries and production JavaScript ceiling before and after this step. elenchus: preserve a failing geometry or recovery state and prove each repair red on the parent and green on the fixed tree. hypomnema: update ADR-005 only if the one-screen or separate-tree decision changes; routine CSS belongs beside the component.

## Step 3: Make each turn model, response and stop

**Goal.** Turn lesson play into one progressive call-and-response card, with a truly quiet route and one optional making card after the core turn.
**Entry.** The accepted Step 2 head, after its complete Exit command passes at all three named viewports.
**Exit.** `npm run verify:local && node scripts/check-child-flow.mjs --candidate one-screen-play-loop --criterion first-response-in-one-action --report .hexaemeron/reports/conformance/one-screen-play-loop--first-response-in-one-action.json && node scripts/check-child-flow.mjs --candidate one-screen-play-loop --criterion every-child-state-one-action-exit --report .hexaemeron/reports/conformance/one-screen-play-loop--every-child-state-one-action-exit.json && node scripts/check-child-quiet.mjs --candidate one-screen-play-loop --criterion quiet-mode-opens-no-audio --report .hexaemeron/reports/conformance/one-screen-play-loop--quiet-mode-opens-no-audio.json` exits zero; sound play models lesson 8 as B-A-A-B and presents one suitable response without a route shelf; quiet play completes a finger or tap turn without `AudioContext`, oscillator, media permission or audible output; **More** alone opens bounded creation; **Done** stops without automatic progression; and every child state retains one-action help or stop.
**Files.** Change `src/App.tsx`, `src/components/ChildStage.tsx`, `src/components/FingeringMission.tsx`, `src/components/PatternMaker.tsx`, `src/components/PatternStrip.tsx`, `src/components/RhythmEcho.tsx`, `src/hooks/useGuideTone.ts`, `src/hooks/useMicrophoneScoring.ts`, `src/lib/mission-loop.ts`, `src/styles.css` and the Step 1 child-copy manifest; create `scripts/check-child-flow.mjs`, `scripts/check-child-quiet.mjs` and `tests/child-flow.test.mjs`; update `README.md`, `docs/verification.md` and the child-first study and runbook only where observed implementation details require it; do not add a persisted field or dependency.
**Tests.** Add exact progressive-state, deterministic card choice, adjacent model/response, optional maker, immediate stop, no-auto-next, neutral feedback, audio mutual exclusion, teardown and quiet-construction refusal cases; retain complete schedule, repeated-note, microphone-denial, reduced-motion and progress tests. The Elenchus runner is `node scripts/emit-node-test-report.mjs {report}`; its report format is `node-test-json-v1`; its report file is `.elenchus/node-test.json`.
**Disciplines.** phylax: oscillator, microphone, quiet mode, lesson data and child actions stay separate, bounded and local. ephoros: visible non-retained mode, playback and teardown state answers recovery questions without analytics. metron: one action to first response and the 300000-byte ceiling are measured by the same commands. elenchus: audio, state or recovery defects require parent-red and fixed-green evidence before the full suite. hypomnema: explain non-obvious mode and audio ordering beside the state machine and record any change to optional creation or natural stopping in ADR-005.

## Step 4: Prove the complete child and deployment paths

**Goal.** Demonstrate the selected design on the supported screens, bind every pending conformance report and verify that the public URL serves a built artifact.
**Entry.** The accepted Step 3 head after its complete Exit command passes. Changing GitHub Pages from legacy branch publication to GitHub Actions additionally requires the user’s separate explicit deployment-setting authorization; without it, stop before that mutation and leave the public-artifact criterion open.
**Exit.** `npm run verify:local && node scripts/check-child-copy.mjs --candidate one-screen-play-loop --criterion rendered-child-copy-approved --report .hexaemeron/reports/conformance/one-screen-play-loop--rendered-child-copy-approved.json && node scripts/check-child-layout.mjs --candidate one-screen-play-loop --criterion small-phone-no-scroll --report .hexaemeron/reports/conformance/one-screen-play-loop--small-phone-no-scroll.json && node scripts/check-child-flow.mjs --candidate one-screen-play-loop --criterion first-response-in-one-action --report .hexaemeron/reports/conformance/one-screen-play-loop--first-response-in-one-action.json && node scripts/check-child-flow.mjs --candidate one-screen-play-loop --criterion every-child-state-one-action-exit --report .hexaemeron/reports/conformance/one-screen-play-loop--every-child-state-one-action-exit.json && node scripts/check-child-quiet.mjs --candidate one-screen-play-loop --criterion quiet-mode-opens-no-audio --report .hexaemeron/reports/conformance/one-screen-play-loop--quiet-mode-opens-no-audio.json && node scripts/check-bundle-budget.mjs --candidate one-screen-play-loop --criterion production-javascript-bytes --report .hexaemeron/reports/conformance/one-screen-play-loop--production-javascript-bytes.json && node scripts/check-live-pages.mjs --candidate one-screen-play-loop --criterion live-pages-boots-built-artifact --report .hexaemeron/reports/conformance/one-screen-play-loop--live-pages-boots-built-artifact.json` exits zero; the in-app browser replay completes one sound turn, one quiet turn and optional making at 320 by 568, 391 by 844 and 768 by 1024; public HTML loads a built hashed asset instead of `/src/main.tsx`; and the manual family checklist still names child read-through, phone/tablet comfort, recorder, room, volume, microphone and learning outcome as unverified until observed.
**Files.** Create `scripts/check-bundle-budget.mjs`, `scripts/check-live-pages.mjs` and any bounded browser harness shared by the conformance scripts; update `scripts/check-static-boundaries.mjs`, `scripts/check-built-boundaries.mjs`, `docs/verification.md` and `README.md`; change `.github/workflows/pages.yml` only if a repo-file defect is found and separately authorised, and never use a workflow edit as a substitute for the required Pages setting.
**Tests.** Add report-schema, tamper, wrong-candidate, wrong-criterion, source-entry, missing-asset, HTTP-failure and bundle-over-ceiling cases; rerun every child-state and legacy guard; record the exact browser and public fetch path. The Elenchus runner is `node scripts/emit-node-test-report.mjs {report}`; its report format is `node-test-json-v1`; its report file is `.elenchus/node-test.json`.
**Disciplines.** phylax: the public fetch accepts only the expected HTTPS origin and bounded same-origin asset path and retains no credentials or child data. ephoros: integration records accepted SHA, build asset digest, deployment identity, public entry digest, fetched asset digest and boot result. metron: the bundle check uses the same post-build byte sum and 300000-byte ceiling as the baseline. elenchus: any deployment or conformance fix preserves the failing response or report and proves the repair before integration. hypomnema: deployment instructions live in `docs/verification.md`; a changed hosting authority or public contract earns an ADR amendment rather than an informal note.
